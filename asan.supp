# Suppress leaks from system libraries and Binary Ninja itself,
# so only leaks from plugins (in ~/.binaryninja/plugins) are reported.

# Ignore system libraries
leak:/usr/lib/lib*
leak:/usr/lib64/lib*
leak:/lib/*
leak:/lib64/*

# Ignore Binary Ninja installation
leak:*/binaryninja/*
leak:/Applications/*
